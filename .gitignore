.build/
clipandnote.app/
*.dmg
.DS_Store
*.swiftpm
.claude/
DerivedData/
*.xcuserstate

# MobileCLIP model build: ignore the venv, checkpoint, and intermediate package.
# The shippable artifacts (Resources/MobileCLIPImage.mlmodelc + clip_labels.json)
# ARE committed so a fresh clone builds an app with the model.
.venv-mc/
Resources/*.pt
Resources/*.mlpackage/
Resources/dmg-background.png
/note_appicon_final.png

# explainer scratch — keep only the two screenshots the site embeds
explainer/*
!explainer/editor.png
!explainer/menu.png
